#include <stdio.h>
#include <math.h>
int main()
{
    int marks,grade;
    scanf("%d",&marks);

    if(marks>=80)
    {grade:'A+';
        printf("Grade:A+",grade);
    }
else
    {
        printf("grade:b",grade);
    }
    return 0;
}
